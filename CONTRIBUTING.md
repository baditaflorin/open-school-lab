# Contributing

Use Node 22 or newer.

1. Run `npm install`.
2. Run `make install-hooks`.
3. Use Conventional Commits.
4. Run `make test`, `make build`, and `make smoke` before pushing.

Mode A means no backend, no runtime secrets, and no GitHub Actions. Keep heavy engines lazy-loaded behind a lab interaction.
