#!/usr/bin/env sh
set -eu

pattern='^(feat|fix|docs|chore|refactor|test|ops|data)(\([a-z0-9._-]+\))?: .+'
if ! grep -Eq "$pattern" "$1"; then
  echo "Commit message must use Conventional Commits, e.g. feat: add lab registry" >&2
  exit 1
fi
